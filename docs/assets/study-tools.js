(()=>{'use strict';
 const KEY='vcs-evidence-collection-v1',ORDER=['Foundations','Jesus Christ','Sacraments','Salvation & Moral Life','Mary & Communion of Saints','Church, Papacy & Authority','Scripture & Canon','Last Things','Early Church & Fathers','Defending the Faith'],LABELS={'Foundations':'I. FOUNDATIONS','Jesus Christ':'II. JESUS CHRIST','Sacraments':'III. THE SACRAMENTS','Salvation & Moral Life':'IV. SALVATION & MORAL LIFE','Mary & Communion of Saints':'V. MARY & COMMUNION OF SAINTS','Church, Papacy & Authority':'VI. CHURCH, PAPACY & AUTHORITY','Scripture & Canon':'VII. SCRIPTURE & CANON','Last Things':'VIII. LAST THINGS','Early Church & Fathers':'IX. EARLY CHURCH & FATHERS','Defending the Faith':'X. DEFENDING THE FAITH'},PATHS=[
  ['Catholic foundations','Revelation, Tradition, Scripture, and the Church',['Foundations','Church, Papacy & Authority','Scripture & Canon']],
  ['Sacraments & salvation','Grace, Baptism, Eucharist, and Christian discipleship',['Sacraments','Salvation & Moral Life']],
  ['Historical Catholic case','Early witnesses, apostolic continuity, and common objections',['Early Church & Fathers','Defending the Faith']]];
 const $=s=>document.querySelector(s),esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
 const read=()=>{try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch{return[]}},write=x=>localStorage.setItem(KEY,JSON.stringify(x));let cate,pastoral;
 const topicButton=t=>`<button type="button" class="study-topic-link" data-study-topic="${esc(t)}">${esc(t)}</button>`;
 function panel(){const anchor=$('.formation-pathways');if(!anchor||$('#studyTools'))return;anchor.insertAdjacentHTML('afterend',`<section id="studyTools" class="study-tools"><div><p class="eyebrow">Guided study</p><h3>Choose a study path</h3><p>Filter related VCS categories, learn the Catechism, or explore Scripture for a life circumstance.</p><p id="pathStatus" class="collection-status" aria-live="polite"></p></div><div class="study-path-buttons">${PATHS.map((p,i)=>`<button type="button" data-path="${i}"><b>${esc(p[0])}</b><span>${esc(p[1])}</span></button>`).join('')}<button type="button" class="study-path-feature" data-study-view="catechism"><b>Learn the Catechism</b><span>The Creed · Sacraments · Life in Christ · Prayer</span></button><button type="button" class="study-path-feature" data-study-view="pastoral"><b>Scripture for life’s circumstances</b><span>Prayerful Scripture and Catholic context for common needs</span></button></div><aside class="evidence-collection"><div><b>My evidence collection</b><span id="collectionCount"></span></div><p id="collectionEmpty">Save topics to gather them here.</p><ol id="collectionList"></ol><div class="collection-actions"><button type="button" class="smallbtn" id="copyCollection">Copy collection link</button><button type="button" class="smallbtn" id="exportCollection">Download study outline</button><button type="button" class="smallbtn" id="clearCollection">Clear collection</button></div><p id="collectionStatus" class="collection-status" aria-live="polite"></p></aside></section><section id="studyFeature" class="study-feature hidden" aria-live="polite"></section>`);
  document.querySelectorAll('[data-path]').forEach(b=>b.onclick=()=>applyPath(PATHS[+b.dataset.path]));document.querySelectorAll('[data-study-view]').forEach(b=>b.onclick=()=>show(b.dataset.studyView));$('#copyCollection').onclick=copy;$('#exportCollection').onclick=exportOutline;$('#clearCollection').onclick=()=>{write([]);renderCollection();decorate()};renderCollection();}
 function applyPath(p){hide();document.querySelectorAll('.topic-section').forEach(s=>{const show=p[2].includes(s.dataset.topicSection);s.hidden=!show;s.classList.toggle('open',show)});const rows=[...document.querySelectorAll('.topic-section:not([hidden]) .topicrow')],refs=rows.reduce((n,r)=>n+Number(r.querySelector('.topicmeta')?.textContent.replace(/\D/g,'')||0),0);$('#topicStats').innerHTML=`<b>${rows.length}</b> topics · <b>${refs}</b> Scripture citations`;$('#pathStatus').textContent=`Showing ${p[0]}: ${rows.length} topics.`;$('#topicGrid').scrollIntoView({behavior:'smooth',block:'start'});}
 function hide(){$('#studyFeature')?.classList.add('hidden')}
 async function load(kind){const old=kind==='catechism'?cate:pastoral;if(old)return old;const name=kind==='catechism'?'catechism_study_path.json':'pastoral_scripture_guide.json',data=await fetch(`data/${name}?v=1.8.1c1.2c`,{cache:'no-store'}).then(r=>{if(!r.ok)throw Error(r.status);return r.json()});if(kind==='catechism')cate=data;else pastoral=data;return data}
 async function show(kind){const host=$('#studyFeature');host.classList.remove('hidden');host.innerHTML='<p class="collection-status">Loading study guide…</p>';$('#pathStatus').textContent='';try{const d=await load(kind);host.innerHTML=kind==='catechism'?cateView(d):pastoralView(d);bindFeature(host);host.scrollIntoView({behavior:'smooth',block:'start'})}catch(e){console.error('Study guide',e);host.innerHTML='<p class="empty">This study guide could not be loaded.</p>'}}
 function cateView(d){return `<div class="study-feature-head"><div><p class="eyebrow">Official learning path</p><h3>${esc(d.title)}</h3><p>${esc(d.source_boundary)}</p></div><a class="button secondary" href="${esc(d.source_documents[0].url)}" target="_blank" rel="noopener">Official Catechism ↗</a></div><div class="catechism-parts">${d.parts.map(p=>`<article class="catechism-part"><div class="catechism-part-head"><div><span class="eyebrow">Part ${p.order} · ${esc(p.ccc_range)}</span><h4>${esc(p.title)}</h4><p>${esc(p.summary)}</p></div><a class="mini-link" href="${esc(p.official_url)}" target="_blank" rel="noopener">${esc(p.official_title)} ↗</a></div><ol>${p.modules.map(m=>`<li><b>${esc(m.title)}</b><span>${esc(m.ccc_range)}</span><div class="study-topic-links">${m.related_topics.map(topicButton).join('')}</div></li>`).join('')}</ol></article>`).join('')}</div>`}
 function scripture(ref){const m=String(ref).match(/^([A-Z0-9]{2,3})\s+(\d+)(?::(\d+))?/);if(!m)return `<span class="refchip muted">${esc(ref)}</span>`;return `<a class="refchip" href="./?${new URLSearchParams({verse:`${m[1]}.${m[2]}.${m[3]||1}`,passage:ref,from:'pastoral-guide'})}#books">${esc(ref)}</a>`}
 function cards(entries,sources){const src=id=>sources.find(x=>x.id===id);return entries.map(e=>`<article class="pastoral-entry ${e.safety_level==='crisis'?'pastoral-crisis':''}"><span class="eyebrow">${e.safety_level==='crisis'?'Urgent-care guidance':'Scripture for life'}</span><h4>${esc(e.title)}</h4><div class="refchips compact">${e.scripture.map(scripture).join('')}</div><p><b>Catholic context:</b> ${e.ccc.map(esc).join(' · ')}</p><div class="study-topic-links">${e.related_topics.map(topicButton).join('')}</div><p class="pastoral-note">${esc(e.pastoral_note)}</p><div class="pastoral-sources">${e.source_ids.map(id=>{const s=src(id);return s?`<a class="mini-link" href="${esc(s.url)}" target="_blank" rel="noopener">${esc(s.title)} ↗</a>`:''}).join('')}</div></article>`).join('')||'<p class="empty">No guide entries match that search.</p>'}
 function pastoralView(d){return `<div class="study-feature-head"><div><p class="eyebrow">Pastoral Scripture guide</p><h3>${esc(d.title)}</h3><p>${esc(d.source_boundary)}</p></div></div><div class="pastoral-search"><label for="pastoralSearch">Find a circumstance</label><input id="pastoralSearch" placeholder="Search anxiety, grief, prayer, family, hope…"></div><div id="pastoralEntries" class="pastoral-entries">${cards(d.entries,d.source_documents)}</div>`}
 function bindFeature(host){host.querySelectorAll('[data-study-topic]').forEach(b=>b.onclick=()=>openTopic(b.dataset.studyTopic));const search=host.querySelector('#pastoralSearch');if(search)search.oninput=()=>{const q=search.value.trim().toLowerCase(),e=(pastoral?.entries||[]).filter(x=>JSON.stringify(x).toLowerCase().includes(q));host.querySelector('#pastoralEntries').innerHTML=cards(e,pastoral.source_documents);bindFeature(host)}}
 function openTopic(title){hide();const q=$('#topicSearch');q.value=title;q.dispatchEvent(new Event('input'));$('#topicGrid').scrollIntoView({behavior:'smooth',block:'start'});setTimeout(()=>$('.topicrow-head')?.click(),100)}
 function renderCollection(){const list=read(),box=$('#collectionList');if(!box)return;$('#collectionCount').textContent=`${list.length} saved`;$('#collectionEmpty').hidden=!!list.length;['#copyCollection','#exportCollection','#clearCollection'].forEach(s=>$(s).disabled=!list.length);box.innerHTML=list.map(x=>`<li><button type="button" data-open="${esc(x)}">${esc(x)}</button><button type="button" aria-label="Remove ${esc(x)}" data-remove="${esc(x)}">×</button></li>`).join('');box.querySelectorAll('[data-open]').forEach(b=>b.onclick=()=>openTopic(b.dataset.open));box.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{write(read().filter(x=>x!==b.dataset.remove));renderCollection();decorate()})}
 async function copy(){const items=read(),status=$('#collectionStatus');if(!items.length)return;const u=new URL(location.href);u.searchParams.set('collection',items.join('|'));try{await navigator.clipboard.writeText(u.href);status.textContent='Collection link copied.'}catch{history.replaceState(null,'',u);status.textContent='Use the address-bar link to share this collection.'}}
 async function exportOutline(){const items=read(),status=$('#collectionStatus');if(!items.length)return;status.textContent='Preparing study outline…';try{const response=await fetch('data/faith_topics.json?v=1.8.1c1.2c',{cache:'no-store'});if(!response.ok)throw Error(response.status);const topics=await response.json(),selected=new Set(items),topicUrl=t=>{const u=new URL('topics.html',location.href);u.searchParams.set('topic',t);return u.href},collectionUrl=()=>{const u=new URL(location.href);u.searchParams.set('collection',items.join('|'));return u.href},lines=['# Veritas Catholica Scripturae — Evidence Collection','',`Exported: ${new Date().toLocaleDateString()}`,'','This outline follows the VCS formation order so new saved topics can be inserted under their matching category and topic heading.','',`[Reopen and continue this collection](${collectionUrl()})`,''];for(const category of ORDER){const group=topics.filter(t=>selected.has(t.topic)&&t.category===category);if(!group.length)continue;lines.push(`## ${LABELS[category]||category}`,'');for(const t of group)lines.push(`### ${t.topic}`,'',t.catholic_summary,'',`- Scripture: ${t.references.map(r=>r.label).join('; ')}`,`- Catechism: ${t.ccc}`,`- [Open this topic in VCS](${topicUrl(t.topic)})`,'');}lines.push('---','','VCS preserves distinctions between Scripture, Catechism, historical research leads, and external formation sources. This export is a study outline, not a substitute for critical editions or official Catholic teaching.','');const blob=new Blob([lines.join('\n')],{type:'text/markdown;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`vcs-evidence-collection-${new Date().toISOString().slice(0,10)}.md`;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),0);status.textContent='Study outline downloaded.'}catch(e){console.error('Study outline export',e);status.textContent='The study outline could not be created. Please try again.'}}
 function decorate(){const chosen=new Set(read());document.querySelectorAll('.topicrow').forEach(row=>{const title=row.querySelector('.topicrow-title')?.textContent?.trim(),head=row.querySelector('.topicrow-head');if(!title||!head)return;let b=head.querySelector('.save-topic');if(!b){b=document.createElement('button');b.type='button';b.className='save-topic';b.onclick=e=>{e.stopPropagation();const x=read(),i=x.indexOf(title);i<0?x.push(title):x.splice(i,1);write(x);renderCollection();decorate()};head.insertBefore(b,head.querySelector('.topicmeta'));}const saved=chosen.has(title),label=saved?'Saved':'Save topic';if(b.textContent!==label)b.textContent=label;if(b.getAttribute('aria-pressed')!==String(saved))b.setAttribute('aria-pressed',String(saved));})}
 function restore(){const c=new URLSearchParams(location.search).get('collection');if(c){write([...new Set(c.split('|').filter(Boolean))]);renderCollection();decorate();$('#collectionStatus').textContent='Shared collection loaded.'}}
 document.addEventListener('DOMContentLoaded',()=>{const grid=$('#topicGrid');if(!grid)return;new MutationObserver(()=>{panel();decorate();restore()}).observe(grid,{childList:true,subtree:true});panel();decorate();restore()});
})();
