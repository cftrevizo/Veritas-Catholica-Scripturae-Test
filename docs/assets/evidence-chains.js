(()=>{'use strict';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function scriptureLink(t,r){if(!r?.canonical)return `<span>${esc(r?.label||'Scripture reference')}</span>`;const p=new URLSearchParams({verse:r.canonical,passage:r.label||r.canonical,topic:t.topic,from:'evidence-chain'});return `<a href="./?${p.toString()}#books">${esc(r.label||r.canonical)}</a>`}
function chain(t){const refs=(t.references||[]).slice(0,3).map(r=>scriptureLink(t,r)).join('');const witnesses=typeof witnessMatch==='function'?witnessMatch(t).slice(0,3):[];const witnessStage=witnesses.length?`<div class="chain-stage chain-witness"><span>Historical witness${witnesses.length>1?'es':''}</span><div>${witnesses.map(s=>`<a href="source-library.html?source=${encodeURIComponent(s.id)}">${esc(s.author)} · ${esc(s.date)}</a>`).join('')}</div></div>`:`<div class="chain-stage chain-witness chain-unavailable"><span>Historical witnesses</span><em>Explore the Source Library for documented research leads.</em></div>`;return `<section class="apologetics-chain" aria-label="Evidence path for ${esc(t.topic)}"><header><p class="eyebrow">C1-C Evidence Path</p><h4>From claim to documented evidence</h4><p>Follow the claim through Catholic teaching, Scripture, and attributable historical research. Historical matches are research leads, not proof-texts by themselves.</p></header><div class="chain-grid"><div class="chain-stage chain-claim"><span>Common claim</span><strong>${esc(t.topic)}</strong></div><div class="chain-stage chain-response"><span>Catholic response</span><p>${esc(t.catholic_summary)}</p></div><div class="chain-stage chain-scripture"><span>Scripture</span><div>${refs||'<em>No linked passage supplied.</em>'}</div></div><div class="chain-stage chain-catechism"><span>Catechism</span><a href="https://www.vatican.va/archive/ENG0015/_INDEX.HTM" target="_blank" rel="noopener">${esc(t.ccc)} ↗</a></div>${witnessStage}<div class="chain-stage chain-search"><span>Connected evidence</span><a href="./?evidence=${encodeURIComponent(t.topic)}#evidence-search">Open Unified Evidence Search →</a></div></div></section>`}
function renderChains(){if(!Array.isArray(topics))return;document.querySelectorAll('.topicrow').forEach(row=>{const t=topics[Number(row.dataset.i)];if(!t||t.category!=='Defending the Faith')return;const body=row.querySelector('.topicrow-body');if(!body||body.dataset.c1cChain==='true')return;const existing=body.querySelector('.evidence-chain');if(existing)existing.outerHTML=chain(t);else body.insertAdjacentHTML('beforeend',chain(t));body.dataset.c1cChain='true'});
}
document.addEventListener('DOMContentLoaded',()=>{const grid=document.querySelector('#topicGrid');if(!grid)return;new MutationObserver(renderChains).observe(grid,{childList:true,subtree:true});renderChains()});
})();
