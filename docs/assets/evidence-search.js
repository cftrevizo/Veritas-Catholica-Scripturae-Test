(()=>{'use strict';
const $=s=>document.querySelector(s), esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let cache=null;
async function load(){if(cache)return cache;const [topics,sources,timeline,ignatius,books]=await Promise.all([
 fetch('data/faith_topics.json').then(r=>r.json()).catch(()=>[]),fetch('data/source_library.json').then(r=>r.json()).catch(()=>[]),fetch('data/timeline.json').then(r=>r.json()).catch(()=>[]),fetch('data/ignatius_study_resources.json?v=1.7.0c1b').then(r=>r.json()).catch(()=>({records:[]})),fetch('data/books.json').then(r=>r.json()).catch(()=>[])]);
cache={topics:Array.isArray(topics)?topics:(topics.topics||[]),sources:Array.isArray(sources)?sources:(sources.records||sources.sources||[]),timeline:Array.isArray(timeline)?timeline:(timeline.events||[]),ignatius:ignatius.records||[],books:Array.isArray(books)?books:[]};return cache}
const text=o=>JSON.stringify(o).toLowerCase();
function top(arr,q,n=8){return arr.filter(x=>text(x).includes(q)).slice(0,n)}
function group(title,items,render){if(!items.length)return `<section class="evidence-group"><h3>${title}</h3><div class="evidence-empty">No matching records.</div></section>`;return `<section class="evidence-group"><h3>${title} <span class="evidence-badge">${items.length}</span></h3><div class="evidence-items">${items.map(render).join('')}</div></section>`}
function topicName(x){return x.title||x.topic||x.name||x.label||'Study Topic'}
function sourceName(x){return [x.author,x.work||x.title].filter(Boolean).join(' — ')||x.label||'Historical source'}
function eventName(x){return x.title||x.label||x.event||'Timeline event'}
function scriptureMatches(books,q){const raw=q.trim(),canon=raw.toUpperCase().replace(/\s+/g,'');let m=canon.match(/^([1-3]?[A-Z]{2,3})[.:](\d+)(?:[.:](\d+))?$/);if(m){const b=books.find(x=>x.code===m[1]);if(b)return [{book:b,chapter:m[2],verse:m[3]||''}]}let mm=raw.match(/^(.+?)\s+(\d+)(?::(\d+))?$/);if(mm){const name=mm[1].toLowerCase(),b=books.find(x=>(x.name+' '+(x.display_name||'')+' '+(x.abbr||'')).toLowerCase().includes(name));if(b)return [{book:b,chapter:mm[2],verse:mm[3]||''}]}return books.filter(b=>(b.name+' '+(b.display_name||'')+' '+(b.abbr||'')+' '+b.code).toLowerCase().includes(raw.toLowerCase())).slice(0,8).map(book=>({book,chapter:'',verse:''}))}
function scriptureHref(x){if(x.chapter&&x.verse)return `./?verse=${encodeURIComponent(`${x.book.code}.${x.chapter}.${x.verse}`)}&passage=${encodeURIComponent(`${x.book.display_name||x.book.name} ${x.chapter}:${x.verse}`)}#books`;return `./?book=${encodeURIComponent(x.book.code)}#books`}
function renderResults(q,d){const v=scriptureMatches(d.books,q),t=top(d.topics,q),s=top(d.sources,q),e=top(d.timeline,q),i=top(d.ignatius,q);const total=v.length+t.length+s.length+e.length+i.length;$('#evidenceSearchStatus').innerHTML=`<b>${total}</b> grouped matches for <b>${esc(q)}</b> · showing up to 8 per evidence layer.`;$('#evidenceSearchResults').innerHTML=
 group('Scripture',v,x=>`<a class="evidence-item" href="${scriptureHref(x)}"><strong>${esc(x.book.display_name||x.book.name)}${x.chapter?' '+x.chapter:''}${x.verse?':'+x.verse:''}</strong><small>Open in the 73-Book Explorer and inspect verified relationships.</small></a>`)+
 group('Study Topics',t,x=>`<a class="evidence-item" href="topics.html?topic=${encodeURIComponent(topicName(x))}"><strong>${esc(topicName(x))}</strong><small>Open the VCS Study Topic and its Scripture, Catechism, and historical evidence.</small></a>`)+
 group('Historical Source Library',s,x=>`<a class="evidence-item" href="source-library.html?source=${encodeURIComponent(x.id||'')}"><strong>${esc(sourceName(x))}</strong><small>${esc(x.era||x.date_label||x.date||'Source Library record')} · VCS historical source</small></a>`)+
 group('Timeline',e,x=>`<a class="evidence-item" href="./?timelineYear=${encodeURIComponent(x.year??x.start_year??'')}&timelineEvent=${encodeURIComponent(x.id||'')}&timelineScope=${encodeURIComponent(x.scope||'')}&timelineImportance=${encodeURIComponent(x.importance||'')}&timelineCategory=${encodeURIComponent(x.category||'')}&timelineLabel=${encodeURIComponent(eventName(x))}#visual-experience"><strong>${esc(eventName(x))}</strong><small>${esc(x.date_label||x.year||x.category||'Historical event')} · Open in Visual Experience</small></a>`)+
 group('Ignatius Catholic Study Bible — Official Free Study Resources',i,x=>`<a class="evidence-item" href="${esc(x.url)}" target="_blank" rel="noopener"><strong>${esc((x.books||[]).join(', ')||x.id)}</strong><small>Ignatius Press official free study guide ↗ · external resource</small></a>`)}
async function run(e){e?.preventDefault();const q=$('#evidenceSearchInput')?.value.trim().toLowerCase();if(!q)return;$('#evidenceSearchStatus').textContent='Searching evidence layers…';renderResults(q,await load())}
document.addEventListener('DOMContentLoaded',()=>{$('#evidenceSearchForm')?.addEventListener('submit',run);const p=new URLSearchParams(location.search).get('evidence');if(p&&$('#evidenceSearchInput')){$('#evidenceSearchInput').value=p;run()}})})();
