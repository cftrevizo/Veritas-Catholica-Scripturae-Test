(()=>{'use strict';let data=null;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));async function load(){if(!data)data=await fetch('data/miracle_recognition_registry.json?v=1.9.0').then(r=>r.json());return data}function attach(){const box=document.querySelector('#evidenceSearchResults'),input=document.querySelector('#evidenceSearchInput');if(!box||!input||box.querySelector('#miracleSearchGroup'))return;const q=input.value.trim().toLowerCase();if(!q)return;load().then(d=>{if(box.querySelector('#miracleSearchGroup'))return;const hits=d.records.filter(r=>JSON.stringify(r).toLowerCase().includes(q));if(!hits.length)return;const html=hits.slice(0,3).map(r=>{const title=r.title||r.person||'Registry record';return `<a class="evidence-item" href="miracle-registry.html?q=${encodeURIComponent(q)}"><strong>${esc(title)}</strong><small><span class="evidence-provenance evidence-provenance-related">Related evidence</span> · Source-linked Miracle Registry record</small></a>`}).join('');box.insertAdjacentHTML('beforeend',`<section id="miracleSearchGroup" class="evidence-group"><h3>The Miracle Registry <span class="evidence-badge">${hits.length}</span></h3><div class="evidence-items">${html}</div></section>`)}).catch(()=>{})}document.addEventListener('DOMContentLoaded',()=>{const box=document.querySelector('#evidenceSearchResults');if(box)new MutationObserver(attach).observe(box,{childList:true});attach()})})();
