let topics=[];const tq=s=>document.querySelector(s);const CCC_URL='https://www.vatican.va/archive/ENG0015/_INDEX.HTM';
function esc(s){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
async function initTopics(){try{topics=await fetch('data/faith_topics.json?v=1.4.0',{cache:'no-store'}).then(r=>{if(!r.ok)throw Error(r.status);return r.json()});let cats=[...new Set(topics.map(t=>t.category))].sort();tq('#topicCategory').innerHTML+=[...cats].map(c=>`<option>${esc(c)}</option>`).join('');renderTopics()}catch(e){console.error('Study topics',e);tq('#topicGrid').innerHTML='<p class="empty">Study topic data could not be loaded.</p>'}}
function renderTopics(){let q=tq('#topicSearch').value.toLowerCase().trim(),c=tq('#topicCategory').value;let x=topics.filter(t=>(c==='all'||t.category===c)&&(!q||(t.topic+' '+t.category+' '+t.ccc+' '+t.catholic_summary+' '+t.references.map(r=>r.label).join(' ')).toLowerCase().includes(q)));let refs=x.reduce((n,t)=>n+t.references.length,0);tq('#topicStats').innerHTML=`<b>${x.length}</b> topics · <b>${refs}</b> Faith Notebook Scripture citations · <b>${x.length}</b> Catechism mappings`;tq('#topicGrid').innerHTML=x.map((t,i)=>`<article class="topiccard"><p class="eyebrow">${esc(t.category)}</p><h2>${esc(t.topic)}</h2><p class="topic-summary">${esc(t.catholic_summary)}</p><div class="topic-source-label">Faith Notebook · Scripture</div><div class="refchips">${t.references.map(r=>r.canonical?`<a class="refchip" href="./?verse=${encodeURIComponent(r.canonical)}#search" title="Open ${esc(r.canonical)} in VCS">${esc(r.label)}</a>`:`<span class="refchip muted">${esc(r.label)}</span>`).join('')}</div><div class="cccbox"><div><span class="topic-source-label">Catechism of the Catholic Church</span><strong>${esc(t.ccc)}</strong></div><a class="ccc-link" href="${CCC_URL}" target="_blank" rel="noopener">Open official Catechism ↗</a></div></article>`).join('')||'<p class="empty">No matching topics.</p>'}
tq('#topicSearch').addEventListener('input',renderTopics);tq('#topicCategory').addEventListener('change',renderTopics);initTopics();
